import {useEffect} from "react";

const Master = (props)=>{
    const {body} = props;
    useEffect(()=>{
        const isLoggedIn = ()=>{}
    },[]);
    return(
        <div>{body}</div>
    );
}

export default Master;